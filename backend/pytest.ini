[pytest]
# Run from the backend/ dir so `import app...` resolves without packaging.
testpaths = tests
python_files = test_*.py
markers =
    integration: hits the live Postgres database (skipped if DATABASE_URL is unreachable)
filterwarnings =
    ignore::DeprecationWarning
